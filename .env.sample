# Copy this file to `.env` and fill in your Gemini credentials
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-flash
# Optional: an HTTP endpoint that accepts POST JSON {"prompt": "..."}
# If unset, the app uses the default Gemini model endpoint.
GEMINI_ENDPOINT=
